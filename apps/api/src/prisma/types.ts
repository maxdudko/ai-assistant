// Temporary types file - remove after running `pnpm prisma generate`
// These types will be available from @prisma/client once the client is regenerated

export enum ConversationState {
  CREATED = 'CREATED',
  ACTIVE = 'ACTIVE',
  ARCHIVED = 'ARCHIVED',
}

export enum ConversationType {
  DAILY = 'DAILY',
  AD_HOC = 'AD_HOC',
}

